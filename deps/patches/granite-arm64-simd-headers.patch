diff --git a/math/simd_headers.hpp b/math/simd_headers.hpp
index 8afa5059..ac3310c3 100644
--- a/math/simd_headers.hpp
+++ b/math/simd_headers.hpp
@@ -22,7 +22,10 @@
 
 #pragma once
 
-#ifdef _MSC_VER
+// clang-cl defines _MSC_VER when targeting ARM64 too, so this has to check the
+// target and not just the compiler, otherwise __SSE__ gets defined on ARM64,
+// arm_neon.h is never included, and the x86 paths below fail to compile.
+#if defined(_MSC_VER) && (defined(_M_X64) || defined(_M_IX86))
 
 #include <intrin.h>
 #if defined(_INCLUDED_PMM) && !defined(__SSE3__)
